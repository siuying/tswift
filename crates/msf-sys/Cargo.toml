[package]
name = "msf-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Raw FFI bindings to the msf (Mini Swift Frontend) C library."
links = "MiniSwiftFrontend"

[build-dependencies]
bindgen = "0.70"
cc = "1.1"
