---
layout: ../../layouts/Doc.astro
title: Foundation
description: Apple Foundation framework implementation status in tswift.
section: status
---

import CoverageBar from '../../components/CoverageBar.astro';
import Mermaid from '../../components/Mermaid.astro';

# Foundation

Foundation is Apple's "base layer" framework — it provides date/time handling,
URL parsing, JSON encoding, data buffers, and more. tswift measures Foundation
coverage through generated `.swiftinterface` inventories and per-type runtime
registries.

**Legend:** ✅ Done · 🟡 Partial · ⬜ Todo · 🛡️ Hardened — behaviour
ground-truthed against the real Swift 6.3 Foundation (probe programs run on the
Apple toolchain, outputs matched byte-for-byte in golden fixtures).

---

## Overall coverage

<CoverageBar label="Foundation" done={313} total={519} />

Member-level coverage from `tools/framework-inventory/coverage.py --framework
foundation`: **313 / 519** public members verified across the in-scope types
(60.3%). The bar tracks members, not whole types. A member is **verified** only
when it is registered in the live runtime *and* exercised by a passing golden
fixture.

---

## How coverage is measured

Foundation coverage is tracked via the **framework inventory** tool
(`tools/framework-inventory/`). It generates a snapshot of every public type and
member in the real Apple Foundation `.swiftinterface`, then compares it against
the runtime's native registry (`frameworks/foundation/registered_keys.txt`). Run
`python3 tools/framework-inventory/coverage.py --framework foundation` for the
live roll-up. See `docs/swift-runtime/stdlib-inventory.md`.

---

## Type status

<Mermaid caption="Foundation type coverage map">
{`
flowchart TD
    done["✅ Mostly done"]
    partial["🟡 Partial"]

    done --> Decimal["Decimal 🛡️\\n38-digit math, ulp, formatted"]
    done --> JSON["JSONEncoder / JSONDecoder 🛡️\\nstrategies, formatting, key conversion"]
    done --> IndexPath["IndexPath\\nconcat, compare, Codable"]
    done --> Measure["Measurement / Unit\\nconvert, arithmetic, Codable"]
    done --> Plist["PropertyListEncoder\\nXML plists"]

    partial --> Data["Data 🛡️\\nbyte ops, base64, Codable"]
    partial --> UUID["UUID\\nComparable, Codable"]
    partial --> IndexSet["IndexSet\\nset algebra, rangeView, shift"]
    partial --> URL["URL / URLComponents 🛡️\\npath ops + percent-encoding"]
    partial --> DateT["Date / Calendar / DateComponents 🛡️\\ndate math, formatted(), ISO8601"]

    style done fill:#1e3a2a,stroke:#6ddf9f,color:#eeeef5
    style partial fill:#2a2512,stroke:#f5d06e,color:#eeeef5
`}
</Mermaid>

---

## Detailed status

Member counts below come from `coverage.py --framework foundation <Type>`.

### F1 — Core value types

| Status | Type | Members | APIs implemented |
|--------|------|---------|----------------|
| 🟡 🛡️ | `Data` | 19 / 35 | byte subscript get/set (validated 0–255), for-in, `replaceSubrange`, `resetBytes`, `range(of:)`, base64 encode/decode |
| 🟡 | `UUID` | 7 / 11 | `init()`, `init(uuidString:)`, `uuidString`, `Comparable` (byte-tuple order), Codable |
| ✅ | `IndexPath` | 18 / 24 | `+`/`+=`, `compare`, for-in, subscript get/set, Codable with strict decode |
| 🟡 | `IndexSet` | 28 / 42 | full set algebra, `rangeView`, `intersects`, `shift`, for-in, `"N indexes"` description |

### F2 — URLs

| Status | Type | Members | APIs implemented |
|--------|------|---------|----------------|
| 🟡 🛡️ | `URL` | 37 / 81 | parsing, `appending(path:/component:)`, path extensions, `standardized`, `resolvingSymlinksInPath` (lexical), Codable |
| 🟡 🛡️ | `URLComponents` | 24 / 35 | all component get/set, `percentEncoded*` accessors with RFC 3986 charset validation, `encodedHost` distinction |
| 🟡 | `URLQueryItem` | 5 / 8 | `name`, `value`, init, equality |

### F3 — Date & time

| Status | Type | Members | APIs implemented |
|--------|------|---------|----------------|
| 🟡 🛡️ | `Date` | 34 / 87 | arithmetic/comparison operators, `formatted()` family (date/time styles, `.dateTime` chain, `.iso8601`), Codable with all four date strategies |
| 🟡 🛡️ | `Calendar` | 37 / 55 | Gregorian/UTC: component extraction, `date(byAdding:)`, `compare(toGranularity:)`, `dateInterval`, `range`, `ordinality`, `nextDate` |
| 🟡 🛡️ | `DateComponents` | 26 / 41 | all components, `date`, `calendar`/`timeZone`, Foundation-exact `description` |

### F4 — Numbers & measurement

| Status | Type | Members | APIs implemented |
|--------|------|---------|----------------|
| ✅ 🛡️ | `Decimal` | 49 / 59 | 38-digit arithmetic, rounding, magnitude constants, `ulp`/`nextUp`/`nextDown`, `isTotallyOrdered`, `formatted()` — constants ground-truthed vs Swift 6.3 |
| 🟡 | `NumberFormatter` | — | basic styles |
| ✅ | `Measurement` / `Unit` | 15 / 20 | value/unit, conversion, arithmetic, Codable |

### F5 — Encoding / decoding

| Status | Type | Members | APIs implemented |
|--------|------|---------|----------------|
| ✅ 🛡️ | `JSONEncoder` | 6 / 8 | `encode` → `Data`, `outputFormatting` (`.prettyPrinted`/`.sortedKeys`), date/data/key strategies (snake_case per Apple's algorithm), nil-omission |
| 🟡 🛡️ | `JSONDecoder` | 5 / 9 | strict `typeMismatch`/`keyNotFound`/`valueNotFound`, typed arrays, all date/data/key strategies |
| ✅ | `PropertyListEncoder` | 3 / 4 | XML plist output (`outputFormat = .xml`), Date/Data/nested containers |

### Out of scope

Platform- and device-specific APIs are intentionally excluded (see
`frameworks/foundation/scope.toml`): `FileManager`, `FileHandle`, `URLSession`,
`URLRequest`, `NSObject`, `Bundle`, and friends. Remaining unimplemented
members are largely locale-dependent format styles, `rangeOf*` string-range
APIs, and resource/bookmark filesystem members.

---

## Hardening

Types marked 🛡️ were validated in dedicated hardening passes: small probe
programs were run on the **real Apple Swift toolchain** to capture ground-truth
outputs, then encoded as golden fixtures (`harden_foundation_*.swift`,
`harden_json.swift`). This process found and fixed real divergences — ISO8601
leap-year/fractional-second validation, `standardized` path edge cases,
percent-encoding charset rules, Decimal precision boundaries, and JSON
strategy-enum API surface.

---

## Roadmap

Next coverage pushes (daily-use weighted):

1. Locale-aware format styles for `Date` / `Decimal` (`FormatStyle` tokens)
2. `URLComponents.rangeOf*` members (needs `Range<String.Index>` bridging)
3. `Calendar.dates` / `enumerateDates` sequence APIs
4. Remaining `Data` / `IndexSet` / `URL` member gaps
